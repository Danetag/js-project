	</div>

	<!-- end content -->

	<footer id="footer">

	</footer>

	<!-- JS -->
	<script>
		
		var NS = NS||{};
		window["NS"] = NS;

		NS.conf = {};

		NS.conf.lang      = "{{lang}}";
		NS.conf.baseUrl   = "<?php echo $baseURL; ?>";
		NS.conf.translate = {
			{{#each translateJS}}
				"{{key}}" : "{{value}}" {{#ifCond @index "<" ../nbItemtranslateJS}} , {{/ifCond}}
			{{/each}}
		};


	</script>

	<script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
    <script>window.jQuery || document.write('<script src="<?php echo $baseURL; ?>/js/vendor/jquery.min.js">\x3C/script>')</script>

	<script src="<?php echo $baseURL; ?>/js/vendor/head.min.js"></script>

    <script>
	    var aScript = [
			{{#each aJS}} 
				{ src : "<?php echo $baseURL; ?>{{src}}" }  {{#ifCond @index "<" ../nbScripts}} , {{/ifCond}}
			{{/each}}
		];
		if( document.documentElement.className.indexOf("lt-ie10") > -1 ) //IE < 10
		{
			
		}
		else
		{

			for(var i in aScript)
			{
				var src = aScript[i].src;
				head.js(src);
			}
		}
	</script>

	</body>
</html>